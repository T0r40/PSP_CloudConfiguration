spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:9018
      retry:
        max-attempts: 10
        initial-interval: 5000
  profiles:
    active: local

